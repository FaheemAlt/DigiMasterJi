# DigiMasterJi Backend Dependencies
# ==================================
# AWS Migration Version

# FastAPI and web framework
fastapi
uvicorn[standard]
python-multipart
httpx
aiofiles

# AWS Lambda ASGI Adapter
mangum

# AWS SDK
boto3
botocore

# Web Search
duckduckgo-search>=8.0.0

# Data validation
pydantic[email]
pydantic-settings

# Authentication
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.3.0

# Environment
python-dotenv

# Background Tasks (Daily Quiz Generation)
# Note: For Lambda, use EventBridge instead of APScheduler
APScheduler

# Speech-to-Text (OpenAI Whisper) - Keep for local STT fallback
# openai-whisper  # Commented out - use Deepgram in production

# Text-to-Speech (Google TTS)
gTTS

# RAG Pipeline - PDF Processing
# Note: Embeddings now handled by Bedrock Titan V2
PyMuPDF==1.24.11
tiktoken==0.7.0

# MongoDB Atlas (for Bedrock KB embeddings stats)
pymongo[srv]

# Audio processing
soundfile
numpy<2.0

# Development tools
pytest
pytest-asyncio
black
flake8